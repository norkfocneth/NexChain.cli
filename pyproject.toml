[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chainsentinel"
version = "1.0.0"
description = "Privacy-first Offline Cryptocurrency Forensics & Threat Intelligence CLI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0.0",
    "typer>=0.9.0",
    "httpx>=0.25.0",
]

[project.scripts]
chainsentinel = "sentinel.cli.main:run_cli"
